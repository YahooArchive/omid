<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>yahoo.yinst.yjava_base_build</groupId>
    <artifactId>yjava_base_build</artifactId>
    <version>3.1.6.153</version>
  </parent>

  <groupId>com.yahoo.omid</groupId>
  <artifactId>omid</artifactId>
  <packaging>pom</packaging>
  <!-- WARNING: do not update version manually, use mvn versions:set on a ylinux machine //-->
  <version>2.2.18</version>
  <name>Omid</name>
  <url>http://www.github.com/yahoo/omid</url>

  <scm>
    <connection>scm:git:git@git.corp.yahoo.com:scalable-computing/omid.git</connection>
    <tag>omid2</tag>
  </scm>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- Java Version -->
    <java.version>1.7</java.version>

    <!-- Yahoo Library Versioning -->
    <yahoo.mon_metrics.version>0.2.11_25</yahoo.mon_metrics.version>

    <!-- 3rd-Party Library Versioning -->
    <hbase.version>0.98.7.3.1410020120_h2</hbase.version>
    <guava.version>11.0.2</guava.version>
    <guice.version>3.0</guice.version>
    <testng.version>6.8.8</testng.version>
    <slf4j.version>1.7.7</slf4j.version>
    <log4j.version>1.2.14</log4j.version>
    <netty.version>3.2.6.Final</netty.version>
    <protobuf.version>2.5.0</protobuf.version>
    <mockito.version>1.9.5</mockito.version>
    <disruptor.version>3.2.0</disruptor.version>
    <metrics.version>3.0.1</metrics.version>
    <jcommander.version>1.35</jcommander.version>
    <commons.conf.version>1.10</commons.conf.version>
    <clover.version>3.3.0</clover.version>
    <hamcrest.version>1.3</hamcrest.version>
    <curator.version>2.6.0</curator.version>
    <statemachine.version>1.0.9</statemachine.version>
  </properties>

  <modules>
    <module>hbase-client</module>
    <module>transaction-client</module>
    <module>tso-server</module>
    <module>commit-table</module>
    <module>hbase-commit-table</module>
    <module>omid-hbase-client</module>
    <module>hbase-coprocessor</module>
  </modules>

  <build>

    <plugins>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>2.9</version>
        <configuration>
          <argLine>-Xmx1G</argLine>
          <forkMode>pertest</forkMode>
          <argLine>-Djava.library.path=${basedir}/lib:/home/y/lib64</argLine>
          <redirectTestOutputToFile>true</redirectTestOutputToFile>
        </configuration>
      </plugin>

      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.1</version>
        <configuration>
          <source>${java.version}</source>
          <target>${java.version}</target>
          <compilerArguments>
            <Werror />
            <Xlint:deprecation />
            <Xlint:unchecked />
          </compilerArguments>
        </configuration>
      </plugin>

      <plugin>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>2.2.1</version>
        <configuration>
          <skipAssembly>true</skipAssembly>
        </configuration>
      </plugin>

    </plugins>

    <pluginManagement>

      <plugins>

        <!--This plugin's configuration is used to store Eclipse m2e settings
        only. It has no influence on the Maven build itself. -->
        <plugin>
          <groupId>org.eclipse.m2e</groupId>
          <artifactId>lifecycle-mapping</artifactId>
          <version>1.0.0</version>
          <configuration>
            <lifecycleMappingMetadata>
              <pluginExecutions>
                <pluginExecution>
                  <pluginExecutionFilter>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <versionRange>[2.1,)</versionRange>
                    <goals>
                      <goal>copy-dependencies</goal>
                    </goals>
                  </pluginExecutionFilter>
                  <action>
                    <ignore></ignore>
                  </action>
                </pluginExecution>
              </pluginExecutions>
            </lifecycleMappingMetadata>
          </configuration>
        </plugin>

      </plugins>

    </pluginManagement>

  </build>

  <profiles>

    <profile>
      <id>consoleTestOutput</id>
      <build>

        <plugins>

          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <configuration>
              <redirectTestOutputToFile>false</redirectTestOutputToFile>
            </configuration>
          </plugin>

        </plugins>

      </build>
    </profile>

    <profile>
      <id>clover</id>
      <build>

        <plugins>

          <plugin>
            <groupId>com.atlassian.maven.plugins</groupId>
            <artifactId>maven-clover2-plugin</artifactId>
            <version>${clover.version}</version>
            <configuration>
              <targetPercentage>${clover.coverage.percent}</targetPercentage>
              <generateHtml>true</generateHtml>
              <generateXml>true</generateXml>
              <baseDir>${project.basedir}</baseDir>
            </configuration>
          </plugin>

        </plugins>

      </build>
    </profile>

  </profiles>

  <repositories>
    <repository>
      <id>local-release</id>
      <url>http://artifactory.ops.yahoo.com:9999/artifactory/maven-local-release/</url>
    </repository>
  </repositories>

</project>
